#include "main.h"
#include <stdio.h>

/**
 * _puts - Function that print the variable with a new line
 *
 * @str: Pointer that points the variable str
 *
 * Return: Nothing
 */
void _puts(char *str)
{
	puts(str);
}
