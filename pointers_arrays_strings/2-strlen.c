#include "main.h"
#include <string.h>

/**
 * _strlen - Function that returns the n lenght of a string
 *
 * @s: Character
 *
 * Return: @size1 value
 */
int _strlen(char *s)
{
	int size1 = strlen(s);

	return (size1);
}
