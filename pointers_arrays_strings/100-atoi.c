#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * _atoi - converts any string into numbers
 * if there's no numbers, return 0
 *
 * @s: string to be converted
 *
 * Return: Always val
 */
int _atoi(char *s)
{
	int val;

	val = atoi(s);
	return (val);
}
