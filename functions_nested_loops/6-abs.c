#include "main.h"
/**
 * _abs - function
 *
 * @n: integer to be evaluated
 *
 * Return: Always the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	else
	{
		return (-n);
	}
}
